/**
MAP CLASS

Aquesta classe representa el mapa on estan els diferents pobles del mon (Cal crear mons?)
*/

#include "../Includes/Includes.hpp"


class GMap {
public:
	vector< vector<Node> > Map (MAP_SIZE, vector<Node> MAX_SIZE);
}
